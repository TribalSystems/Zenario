Bugfix: Removed the trailing comma on line 69

Add the ability to override the label, even if something is selected

	Replace:
            if( instance.options.texts.placeholder ) {
                placeholder.text( instance.options.texts.placeholder );
            }
	With:
            if( instance.options.texts.overridePlaceholder ) {
                placeholder.text( instance.options.texts.overridePlaceholder );
            } else
            if( instance.options.texts.placeholder ) {
                placeholder.text( instance.options.texts.placeholder );
            }




	Replace:
            // replace placeholder text
            else {
                placeholder.text( instance.options.texts.placeholder );
            }
	With:
            // replace placeholder text
            else {
                placeholder.text( instance.options.texts.placeholder );
            }
            if( instance.options.texts.overridePlaceholder ) {
                placeholder.text( instance.options.texts.overridePlaceholder );
            }
