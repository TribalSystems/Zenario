# You must use a tab-size of 4 spaces to edit this file.
---
zenario_alias:
    tabs:
        meta_data:
            fields:
                zenario_pro_features__create_spare_alias:
                    priv: _PRIV_MANAGE_SPARE_ALIAS
                    label: 'Create a spare alias under the old name'
                    type: checkbox
                    side_note: |
                        Don't lose traffic.
                        When visitors request the page's old alias, the server will perform a Redirect to its new alias (recommended).
                    visible_if: zenarioAB.editModeOn()
