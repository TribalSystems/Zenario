# You must use a tab-size of 4 spaces to edit this file.
---
author_name: Tribal Systems
copyright_info: © Tribal Limited
editions: Community, Pro, ProBusiness, Enterprise
start_running_on_install: true  # N.B. changing this property won't do anything as this module is hard-coded in the installer to run on install.
license_info: 'Modified BSD License'
display_name: Menu (Vertical)
description: |
  <p>Displays a portion of the hierarchical menu of a Zenario website, using a vertical list.</p>
  <p>This menu is often used as a side menu of a site, but when the related menu section doesn't contain many items it can be placed in the header or footer.</p>
  <p>It can generate different types of menu, including "popout" menus (which appear upon mouseover).</p>
  <p>It has a handy feature on extranet sites where it can display, "Welcome!" and the name of the logged in user.</p>
  <p>For a horizontal navigation bar, use a plugin of the Menu (Horizontal) module.</p>
category: pluggable
keywords: menu, navigation

# Reminder: you need to increment the LATEST_REVISION_NO in this module's
# latest_revision_no.inc.php file if you change any of the fields below:
css_class_name: zenario_menu_vertical
is_pluggable: true
can_be_plugged_into:
    content_items: false
    layouts: true
    regular_nests: true
    slideshows: false
    ajax_nests_without_conductor: true
    ajax_nests_with_conductor: false
default_framework: standard
default_instance_name: Vertical Menu

# Warning: adding a new dependency to an existing module will break any existing sites
# where the dependency is not running.
# You can fix this on a per-site basis by manually running the dependency, i.e. by running
#   UPDATE zenario_modules SET status = 'module_running' WHERE class_name = 'dependency'
# in MySQL and then going to /admin/ to apply database updates.
# If this is a core module, consider using runNewModuleDependency() in a 
# database-revision in zenario/admin/db_updates/step_4_migrate_the_data/plugins.inc.php
# to fix it automatically for every site.
dependencies:
    zenario_menu: true
inheritance:
    inherit_settings_from_module: zenario_menu
