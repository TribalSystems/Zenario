# You must use a tab-size of 4 spaces to edit this file.
---
zenario__administration:
    panels:
        site_settings:
            columns:
                _unused:
                    hidden: true
            items:
                ip_services:
                    name: GeoIP services
                    desc: Settings for GeoIP services.
                    keywords: MaxMind, IP, location, GeoIP
