# You must use a tab-size of 4 spaces to edit this file.
---
zenario_email_template:
    tabs:
        body:
            fields:
                user_form:
                    ord: 2
                    label: Insert merge fields from a form
                    type: select
                    empty_value: -- Select Form --
                    format_onchange: true
                    side_note: |
                        Add forms fields and their merge fields onto the current email body by selecting a form and then choosing all or individual form fields
                user_form_field:
                    ord: 3
                    same_row: 1
                    type: select
                    empty_value: -- Select Field --
                    format_onchange: true
                    hidden: true
