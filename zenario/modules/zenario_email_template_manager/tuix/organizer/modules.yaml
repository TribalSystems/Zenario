zenario__modules:
    panels:
        modules:
            item_buttons:
                email_templates:
                    ord: 4.5
                    parent: view_dropdown
                    label: "Module's email templates"
                    hide_in_select_mode: true
                    link:
                        path: zenario__email_template_manager/panels/email_templates
                        branch: true
                        refiner: module
                    visible_if_for_all_selected_items: item.status != 'module_not_initialized'