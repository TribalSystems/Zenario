# You must use a tab-size of 4 spaces to edit this file.
---
zenario__logout:
    key:
        id: 0
        openFromAdminToolbar: false
    title: Logout
    tabs:
        logout:
            edit_mode:
                enabled: true
            fields:
                #desc:
                #    full_width: true
                #    snippet:
                #        html:
                options:
                    label: 'Logout options:'
                    type: checkboxes
                    value: extranet_user
                    values:
                        extranet_user:
                            label: Logout of extranet user Fred Smith's account
                        admin:
                            label: Logout as an administrator
                    validation:
                        required: Please select an option
    max_height: 150
    hide_tab_bar: true
    save_button_message: Logout
    cancel_button_message: Cancel
